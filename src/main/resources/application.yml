spring:
    application:
        name: ${artifactId}
        version: ${version}
    jmx.enabled: true


jwt:
    token:
        issuer: "\${JWT_TOKEN_ISS:https://dev.selfcare.pagopa.it}"
        audience: "\${JWT_TOKEN_AUD:dev.piattaformaunitaria.pagopa.it}"
        jwk: "\${JWT_TOKEN_JWK:https://dev.selfcare.pagopa.it}"


management:
    endpoint:
        health:
            probes.enabled: true
            group:
                readiness.include: "*"
                liveness.include: livenessState,diskSpace,ping
    endpoints:
        jmx:
            exposure.include: "*"
        web:
            exposure.include: info, health